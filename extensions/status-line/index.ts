/**
 * Crumbs status line footer for Pi.
 *
 * - Replaces Pi's built-in footer with two compact lines.
 * - Shows branch/model, context/token/cache stats, cwd/session name, and caveman state.
 * - Run `/status-line` to toggle it on or off.
 *
 * Example:
 * `/status-line`
 */

export { default } from "./src/extension.js";
